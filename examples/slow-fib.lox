fun fib(n) {
    if (n < 2) return n;
    return fib(n - 1) + fib(n - 2);
}

var before = clock();
print "fib(40) == " + fib(40);
var after = clock();
print "// This took " + (after - before) + " seconds to calculate."
